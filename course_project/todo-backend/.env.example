# Local development environment variables
DATABASE_URL=postgresql+asyncpg://todouser:todopass@localhost:5432/todoapp

# PostgreSQL connection details (alternative to DATABASE_URL)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=todoapp
POSTGRES_USER=todouser
POSTGRES_PASSWORD=todopass

# Application settings
DEBUG=  # Leave empty for auto-detection (true for dev, false for production)
LOG_LEVEL=INFO
SQL_DEBUG=false

# API settings
API_TITLE=Todo Backend API
API_DESCRIPTION=Backend service for todo application
API_VERSION=1.0.0
HOST=0.0.0.0
PORT=8000

# CORS settings
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
