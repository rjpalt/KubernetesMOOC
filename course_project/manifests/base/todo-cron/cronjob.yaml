apiVersion: batch/v1
kind: CronJob
metadata:
  name: todo-cronjob
  namespace: project
spec:
  # Runs every minute
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: todo-cronjob
              image: kubemooc.azurecr.io/todo-app-cron:3.5-amd64
              imagePullPolicy: IfNotPresent
              env:
                - name: TODO_BACKEND_URL
                  value: "http://todo-app-be-svc:2506"
                - name: LOG_LEVEL
                  value: "INFO"
                - name: WIKIPEDIA_RANDOM_URL
                  value: "https://en.wikipedia.org/wiki/Special:Random"
