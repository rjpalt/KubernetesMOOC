apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-fe
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app-fe
  template:
    metadata:
      labels:
        app: todo-app-fe
    spec:
      initContainers:
        - name: fix-cache-permissions
          image: busybox
          command: ['sh', '-c', 'chmod 777 /images']
          resources:
            requests:
              cpu: "50m"      # 0.05 CPU cores
              memory: "32Mi"  # 32 MB RAM
            limits:
              cpu: "100m"     # 0.1 CPU cores max
              memory: "64Mi"  # 64 MB RAM max
          volumeMounts:
            - name: image-cache-volume
              mountPath: /images
      volumes:
        - name: image-cache-volume
          persistentVolumeClaim:
            claimName: image-cache-pvc
      containers:
      - name: todo-app-fe
        # WARNING: This image tag is automatically updated by CI/CD pipeline  
        # DO NOT manually edit - tag gets replaced with commit SHA during deployment
        image: kubemooc.azurecr.io/todo-app-fe:PLACEHOLDER-UPDATED-BY-CICD
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        env:
        - name: PORT
          value: "8000"
        - name: TODO_BACKEND_URL
          value: "http://todo-app-be-svc:2506"
        - name: LOG_LEVEL
          value: "INFO"
        - name: API_BASE_PATH
          value: "/"
        volumeMounts:
          - name: image-cache-volume
            mountPath: /images