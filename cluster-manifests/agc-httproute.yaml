apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-route-example
  namespace: agc-test
  annotations:
    description: "Example HTTPRoute for testing AGC feature gateway - replace with actual feature branch routes"
spec:
  parentRefs:
  - name: agc-feature-gateway
    namespace: agc-shared
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nginx-test
      port: 80
